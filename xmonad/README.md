# Xmonad 

copy xmonad config to xmonad directory
```bash
sudo cp xmonad.hs ~/.config/xmonad/
```
copy xmobar config to xmonad directory
```bash
sudo cp xmobarrc ~/.config/xmonad/
```
dunst config 
```bash
sudo cp dunstrc ~/.config/dunst/
```

